name: quickstart/introduction
version: 0.0.@
description:
logo:

depends:
  - quickstart/hello-escpae-latest as hello

inputs:
  - id: literal
    default: $hello.outputs.literal
    eval_before_dependencies: false
